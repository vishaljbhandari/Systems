#!/bin/bash

 $1 << EOF

CREATE OR REPLACE VIEW ACCOUNT_PRECHECK_V
AS
(
 SELECT * FROM ACCOUNT WHERE ACCOUNT_TYPE=2
) ;


CREATE OR REPLACE VIEW SUBSCRIBER_PRECHECK_V
AS
(
 SELECT * FROM SUBSCRIBER WHERE SUBSCRIBER_TYPE=2
) ;

commit ;
EOF
