                    {
                        ulSize = sizeof(VOLMTINFO) + ulCurrent * sizeof(PVOLMTINFOPARENT);
                        pVolMt = REALLOC(pVolMt, ulSize);
                        if(pVolMt == NULL)
                            __leave;
