
                dwBufferSize += sizeof(DISK_EXTENT);
                lpOutBuffer = (BYTE *) REALLOC (lpOutBuffer,
                                        dwBufferSize * sizeof (BYTE));
            }
