                {
                    barLen   += ALLOC_SIZE;
                    barNames  = (tchar **)REALLOC(barNames, barLen*sizeof(tchar *));
                }
                barNames[barNum++] = StrNewCopy(barName);
